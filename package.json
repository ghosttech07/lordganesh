{
  "name": "mushak-dash",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Ganesh & Unlimited Modak — a 3D open-world collection game",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
